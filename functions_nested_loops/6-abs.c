#include "main.h"

/**
 * _abs - locura tricolor
 * @n: numero al cual se le va a buscar su valor abs
 * Return: valor absoluto del numero
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
